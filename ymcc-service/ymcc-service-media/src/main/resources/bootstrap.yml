spring:
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: dev
  application:
    name: service-media
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #注册中心
      config:
        server-addr: localhost:8848 #配置中心
        file-extension: yaml #配置文件格式
        prefix: application-media #配置前缀 ，默认使用sring.application.name
        group: DEFAULT_GROUP
        namespace: 4b73d058-71a2-44f4-80e7-1620c71cf2f1